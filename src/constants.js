// Exaggerated scale (default) — sizes enlarged, distances compressed
export const EXAGGERATED_SCALE = {
  sunRadius: 20,
  earthRadius: 5,
  moonRadius: 1.4,
  earthOrbitRadius: 200,
  moonOrbitRadius: 30,
};

// Accurate scale — true ratios normalized to Earth radius = 1
export const ACCURATE_SCALE = {
  sunRadius: 109.2,
  earthRadius: 1,
  moonRadius: 0.273,
  earthOrbitRadius: 23481,
  moonOrbitRadius: 60.3,
};

// Default simulation settings
export const DEFAULT_SPEED = 1;
export const EARTH_AXIAL_TILT = 23.4; // degrees
